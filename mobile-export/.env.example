# Public domain that hosts your deployed web + API server (no protocol).
# Example: jumpthebook.example.com  (the mobile app will call https://<this>/api/*)
EXPO_PUBLIC_DOMAIN=

# Clerk publishable key (same one your web app uses).
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
